{{if data.mode == 0}} <!-- Main Menu -->
    <div class="item" style="text-align: center;">
		<h2>Main Menu</h2>
    </div>
    <!-- Toggle Psychoscope -->
    <div class="itemContent">
        {{:helper.link('Toggle Psychoscope', 'lightbulb', { 'option': 'togglePsychoscope' }, null)}}
    </div>
    <!-- Show Scans Journal -->
    <div class="itemContent">
        {{:helper.link('Scans Journal', 'clipboard', { 'option': 'showScansJournal' }, null)}}
	</div>
	<!-- Show Lifeforms List -->
	<div class="itemContent">
		{{:helper.link('Species List', 'dna', { 'option': 'showLifeformsList' }, null)}}
	</div>
{{else data.mode == 1}}
	<h2>Psychoscope Scans Journal</h2>
        <!-- List of scans -->
	{{for data.scans_journal}}
		<h3>Scan of "{{:value.scan.object_name}}"</h3>
		<b>Date: </b> {{:value.scan.date}}
		<br>
		<b>Kingdom:</b> {{:value.scan.lifeform.kingdom}}
		<br>
		<b>Class:</b> {{:value.scan.lifeform.class}}
		<br>
		<b>Genus:</b> {{:value.scan.lifeform.genus}}
		<br>
		<b>Species:</b> {{:value.scan.lifeform.species}}
		<br>
		<div class="itemContent">
			{{:helper.link('Show Details', 'search', {'option': 'showLifeform', 'lifeform_reference': value.lifeform_reference}, null)}}
		</div>
		<div class="itemContent">
			{{:helper.link('Delete Scan', 'trash', {'option': 'deleteScan', 'scan_reference': value.scan_reference}, null, 'redButton')}}
		</div>
		<br>
    {{empty}}
        <b>No scans detected.</b>
    {{/for}}
	<!-- Return to Main Menu -->
	<br>
	{{:helper.link('Close', 'arrow-left', { 'option': 'showMainMenu' }, null)}}

<!-- Species List -->
{{else data.mode == 2}}
	<h2>Species List</h2>
	{{for data.lifeforms_list}}
		<br>
		<div class="itemContent">
			{{:helper.link(value.lifeform.genus, 'search', {'option': 'showLifeform', 'lifeform_reference': value.lifeform_reference}, null)}}
		</div>
		<br>
	{{empty}}
		<b>No known lifeforms.</b>
	{{/for}}
	<br>
	{{:helper.link('Close', 'arrow-left', { 'option': 'showMainMenu' }, null)}}
<!-- Lifeform Details -->
{{else data.mode == 3}}
	{{if data.lifeform}}
		<h2>Details of "{{:data.lifeform.species}}"</h2>

		<b>Kingdom:</b> {{:data.lifeform.kingdom}}
		<br>
		<b>Class:</b> {{:data.lifeform.class}}
		<br>
		<b>Genus:</b> {{:data.lifeform.genus}}
		<br>
		<b>Species:</b> {{:data.lifeform.species}}
		<br>
		<b>Description:</b> {{:data.lifeform.desc}}
		<br>
		<b>Scans: </b> {{:data.lifeform.scan_count}}
		<br>
		<!-- TECHNOLOGIES LIST -->
		<h3>New Technologies:</h3>
		{{for data.lifeform.opened_techs}}
			<b>{{:value.tech_name}}</b> - {{:value.tech_level}}
			<br>
		{{empty}}
			<b>No new technologies.</b>
		{{/for}}
		<br>
		{{if data.lifeform.opened_techs}}
			<div class="itemContent">
				{{:helper.link('Print Technology Data', 'print', { 'option': 'printTechs', 'lifeform_reference': data.lifeform_reference }, null)}}
			</div>
			<br>
		{{/if}}
		<!-- NEUROMODS LIST -->
		<h3>New Neuromods:</h3>
		{{for data.lifeform.opened_neuromods}}
			<b>{{:value.neuromod_name}}</b> - {{:value.neuromod_desc}}
			<br>
			{{:helper.link('Print Neuromod Disk Data', 'print', { 'option': 'printNeuromodData', 'neuromod_type': value.neuromod_type }, null)}}
			<br>
		{{empty}}
			<b>No new neuromods.</b>
		{{/for}}
		<br>
	{{else}}
		<b>Error! Lifeform not found!</b>
		<br>
	{{/if}}

	<div class="itemContent">
		{{:helper.link('Close', 'arrow-left', { 'option': 'back' }, null)}}
	</div>
{{/if}}